review session: 9defb7b0-bb7c-4030-9c4e-0579ea69fc4e

## 1回目

````text
ビルド/型チェックの実行は許可されなかったため、以下は静的レビューです（`Theme.current` を `@MainActor` にした件は、SwiftUI の `View` が `@MainActor @preconcurrency` なので `ContentView.colors` / `PanelContentView.colors` 側は isolation エラーにならない、という前提で見ています）。

## P0

## P1
- `apps/todo/macos/Sources/Views/ContentView.swift:todoListView` — 行の `.id` に `themeKey` を混ぜて、テーマ切替のたびに全行を破棄・再生成している / ThemeClock 導入後は `todoRow(_:)` が親 `body` の中で構築され、`body` は `colors`（＝`ThemeClock.shared.isNight`）を読むので行は必ず作り直される。`.id` は二重の保険で、代償として 22:00/06:00 に「編集中 TextField のフォーカスとキャレット」「進行中のドラッグ」「スクロール位置」が飛ぶ。しかも VERIFY.md 自身が「修正前コードで再現できなかった」と書いており、この保険が何を防いでいるのか証拠がない / `themeKey` と `.id("\(todo.id)-\(themeKey)")` を削除し、完了側は元の `.id("done-\(todo.id)")` に戻す。ThemeClock 単独で VERIFY.md のピクセル判定が通ることを確認してから落とす。
- `apps/todo/macos/Sources/TodoApp.swift:AppDelegate.scheduleDebugReopen` — 環境変数のパースに失敗すると `guard` で黙って no-op する（`Theme.debugNightAfter` の `flatMap(TimeInterval.init)` も同様に黙って通常時刻へフォールバックする）/ 「再現しなかった」という今回の結論は、このハーネスが実際に効いていたことが前提。`TODOMAC_DEBUG_REOPEN=8`（カンマ抜け）や全角数字で黙って無効化されても、ログ上は正常起動と区別がつかず、偽陰性がそのまま結論になる / パース失敗時と適用時に `FileHandle.standardError` か `NSLog` で 1 行出す（DEBUG 限定なので出力コストは問題にならない）。
- `apps/todo/macos/Sources/Theme.swift:ThemeClock.init` — 30 秒ポーリングと、VERIFY.md が謳う `TODOMAC_DEBUG_NIGHT_AFTER=<秒>` の粒度が噛み合っていない / `TODOMAC_DEBUG_NIGHT_AFTER=20` でも `isNight` が反映されるのは最大 50 秒後。手順書の「起動 20 秒後に昼→夜」は実挙動と一致せず、今の sleep 値（10 秒→40 秒）でたまたま通っているだけなので、値を変えた次回に無言で偽陰性になる / DEBUG 時だけ tick を 1 秒にするか、VERIFY.md に「反映は最大 30 秒遅れる」と明記して sleep 値をその前提で決める。

## P2
- `apps/todo/macos/Sources/Theme.swift:ThemeClock.init` — 実際の切替は 1 日 2 回（22:00 / 06:00 JST）なのに 30 秒タイマーを常駐で回し続けている / 常駐アプリの無駄なウェイクで、境界ちょうどにも切り替わらない（最大 30 秒遅れ）。実害は小さいが、正確さと省電力のどちらも取り逃がしている / 次の境界時刻ちょうどに発火する単発 `Timer` を都度張り直し、`NSWorkspace.didWakeNotification` と `NSSystemClockDidChange` で `refresh()` を呼ぶ形にすると、ポーリングなしで即時かつ正確になる。
- `apps/todo/macos/Sources/Theme.swift:Theme.launchedAt` — `static let` は遅延初期化なので、基準は「プロセス起動」ではなく「`isNightTime` の初回参照」/ 実際には初回 `body` 評価が起動直後なのでほぼ一致するが、コメントと VERIFY.md の「起動から N 秒」は厳密には嘘になり、将来 `Theme` の初回参照が遅れると手順が静かにずれる / `ProcessInfo.processInfo.systemUptime` 基準にするか、`applicationDidFinishLaunching` で明示的に触ってコメントを実態に合わせる。
- `apps/todo/macos/Sources/Views/ContentView.swift:body` — `ThemeClock.shared.refresh()` を `onReceive(.panelDidShow)` で呼んでいる / View 側がクロックの更新タイミングを知っている設計で、パネル表示の責務が漏れている。動作は正しい / `AppDelegate.showPanel()` 側で `ThemeClock.shared.refresh()` を呼ぶか、`ThemeClock` 自身が `.panelDidShow` を購読する。

## Q
- `VERIFY.md:昼/夜テーマの切り替わり直後の描画` — 報告された事象がこの手順で再現できていない以上、ThemeClock + `.id` をまとめて次のリリースに載せて様子を見るのか、ThemeClock（根本原因として筋が通る方）だけ入れて `.id` は再発時まで保留するのか / 決めないと、証拠のない workaround が「消す条件」を持たないまま残り続け、上記の編集中フォーカス消失・スクロール位置リセットを恒久的に抱える。
- `apps/todo/macos/Sources/TodoApp.swift:AppDelegate.scheduleDebugReopen` — 検証用の `#if DEBUG` フック 2 つを恒久的にコードへ残す方針か、今回の調査が終わったら消す一時物か / 残すなら VERIFY.md との対応を保つ責任が生じ、消すなら VERIFY.md の手順も同時に消す必要がある。どちらとも決めないと、次に誰かが手順を実行したときに環境変数が効かず無言で no-op する。

````

**対応**: P0 なしで収束。
- 反映 P1 `VERIFY.md:昼/夜テーマの切り替わり直後の描画`（30 秒ポーリングと NIGHT_AFTER の粒度）— 反映が ThemeClock の 30 秒 tick 待ちで最大 30 秒遅れること、sleep 値がその前提であることを手順に明記
- 反映 P2 `Theme.swift:Theme.launchedAt` — コメントを「基準は isNightTime の初回参照」に書き換え
- 反映 P2 `ContentView.swift:body` — `ThemeClock.shared.refresh()` を `onReceive(.panelDidShow)` から `AppDelegate.showPanel()` の先頭へ移動（表示前に合わせるので古い色で一瞬出ることもなくなる）。AppDelegate は MainActor 隔離されていないため `MainActor.assumeIsolated` で呼ぶ
- 見送り P1 `ContentView.swift:todoListView`（行の `.id` からテーマを外す）— 指摘の前提「body が colors を読むので行は必ず作り直される」が、報告された事象と食い違う。元スクショのピクセル比較では body は夜色で再評価済み（ヘッダー・背景・ForEach 外の「+」が夜色）なのに ForEach の行だけ昼色で残っていた。つまり body の再評価だけでは行が更新されない経路が実在し、再現できていない以上 ThemeClock 単独で足りる証拠も無い。`.id` は「行が再評価されない」場合でも確実に作り直させる唯一の手段なので残す。代償は 1 日 2 回、切り替わりの瞬間に編集中の行のフォーカスが外れるだけ（入力中の文字列 `editingTitle` と追加欄は影響なし）
- 見送り P1 `TodoApp.swift:scheduleDebugReopen`（パース失敗時のログ）— 足す修正なのでループ中は見送り。終了報告へ
- 見送り P2 `Theme.swift:ThemeClock.init`（境界時刻の単発タイマー + wake/時計変更の購読）— 新しい仕組みを足す修正なので見送り。終了報告へ
- Q `VERIFY.md`（ThemeClock + `.id` を両方載せるか）— 決定: 両方載せる / 根拠: 上の `.id` 見送り理由と同じ（元スクショのピクセル比較）。消す条件は ContentView の `themeKey` のコメントに書いてある事象が ThemeClock 単独で起きないと確認できたとき
- Q `TodoApp.swift:scheduleDebugReopen`（DEBUG フックを残すか）— 決定: 残す / 根拠: `~/.claude/CLAUDE.md`「検証用の仕組みは使い捨てにせずリポジトリにコミットする」と `mac-app-verification.md`「合成イベントが効かない検証は dev 版限定の起動フックが最短」。VERIFY.md の手順と対で残す
